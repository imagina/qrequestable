import { reactive } from '@vue/composition-api';

const state = reactive({
    informationModal: {},
});

export default function qRequestableStore() { 
    /* start of mutualization set and get*/

    /* end mutaalization set and get */
    /* stars actions  Promise */

    /* end actions */
    return {
    }
}